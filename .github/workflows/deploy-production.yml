name: Deploy to Production

on:
  push:
    branches: [master]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }} # doossee/vet-hab-ai-frontend

concurrency:
  group: deploy-vethab-frontend
  cancel-in-progress: false

jobs:
  build-and-push:
    name: Build & Push Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest
            type=sha,prefix=prod-

      - name: Build & push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile.prod
          target: production
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            BASE_URL=${{ vars.BASE_URL || 'http://old-backend:3000' }}
            NEXT_PUBLIC_BASE_URL=${{ vars.NEXT_PUBLIC_BASE_URL || 'https://api-old.vet-diagnostics.uz' }}
            NEXT_PUBLIC_PREDICT_BASE_URL=${{ vars.NEXT_PUBLIC_PREDICT_BASE_URL || 'https://api-old.vet-diagnostics.uz/predict' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  trigger-deploy:
    name: Trigger deploy on ops repo
    runs-on: ubuntu-latest
    needs: build-and-push
    steps:
      - name: Dispatch old frontend deploy to vet-diagnostics-deploy
        env:
          GH_TOKEN: ${{ secrets.OPS_DISPATCH_TOKEN }}
        run: |
          curl -fsSL -X POST \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            https://api.github.com/repos/doossee/vet-diagnostics-deploy/dispatches \
            -d "{\"event_type\":\"deploy-old-frontend\",\"client_payload\":{\"sha\":\"${{ github.sha }}\"}}"
          echo "Triggered old frontend deploy on the central ops repo."
